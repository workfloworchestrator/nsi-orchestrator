apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsi-orchestrator.fullname" . }}-test-connection"
  labels:
    {{- include "nsi-orchestrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-q', '-O', '/dev/null', 'http://{{ include "nsi-orchestrator.fullname" . }}:{{ .Values.service.port }}/api/health/']
  restartPolicy: Never
